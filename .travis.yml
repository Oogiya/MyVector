dist: trusty
language: cpp
os: linux
sudo: required
compiler: g++
services:
        - docker
branches:
        only:
                - Template_Migration
addons:
        apt:
                sources:
                        - ubuntu-toolchain-r-test
                packages:
                        - g++-6
                        - cmake
install:
        - docker build -t https://github.com/oogiya/myvector
before_script:
        - docker build --tag development -f Dockerfile.dev . 
script: 
        - docker run https://github.com/oogiya/myvector
        - g++ -v && cmake --version
